# Environment Variables Configuration

# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT="https://your-resource.openai.azure.com/"
AZURE_OPENAI_API_KEY="your-azure-openai-key"
AZURE_OPENAI_DEPLOYMENT="gpt-4"

# Application Settings
ASPNETCORE_ENVIRONMENT="Development"
DOTNET_ENVIRONMENT="Development"

#LOGGING_LEVEL="Debug"  # Options: Trace, Debug, Information, Warning, Error, Critical, None
# Optional: Custom Agent Settings
#AGENT_THINK_INTERVAL="5"  # seconds between thoughts
#AGENT_MAX_THOUGHTS="100"  # maximum thoughts to keep in memory
#AGENT_MAX_ACTIONS="50"    # maximum actions to keep in memory

# Cost Control Settings (for gpt-4o-mini at $0.15 per 1M tokens)
# Target: Keep daily costs under $2/day
# Calculation: $2 / $0.15 per 1M tokens = 13.33M tokens/day
# With safety margin (60%): 8M tokens/day ≈ 4,000 loops/day (assuming 2K tokens per loop)
#AGENT_MAX_LOOPS_PER_DAY="4000"      # maximum agent loops per day (~$1.20/day)
#AGENT_MAX_TOKENS_PER_DAY="8000000"  # maximum tokens per day (~$1.20/day)
